# Chatbot Backend (FastAPI)
This is the backend repository for the AI Chatbot.

## Setup
1. `pip install -r requirements.txt`
2. `python -m uvicorn backend.main:app --reload`

## Deployment
Deployed on Vercel (recommended) or Render.
